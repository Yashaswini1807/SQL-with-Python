# SQL-with-Python
This repository features Python code for sales data analysis using SQLite. It stores, retrieves, and analyzes sales data with SQL queries, then visualizes the results in charts (bar, scatter, line).
